package org.sbrubbles.conditio.fixtures;

import org.sbrubbles.conditio.Scope;

public class SonOfSkipHandler extends SkipHandler {
  public SonOfSkipHandler(Scope scope, Entry value) {
    super(scope, value);
  }
}
